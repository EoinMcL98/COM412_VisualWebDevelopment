@page
@model CinemaBooking.Pages.Actor.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Actors[0].ActorName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Actors[0].FilmID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Actors[0].Title)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Actors) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ActorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FilmID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ActorID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ActorID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ActorID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
